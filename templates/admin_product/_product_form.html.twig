    {{form_start(form)}}

            <div class="row form_product">
                <div class="col-md-6 form_field ">{{form_row(form.productName)}}</div>
                <div class="col-md-6 form_field">{{form_row(form.productDescription)}}</div>
                <div class="col-md-6 form_field">{{form_row(form.productPrice)}}</div>
                <div class="col-md-6 form_field">{{form_row(form.imageFile)}}</div>
                <div class="col-md-6 form_field">{{form_row(form.categories)}}</div>
                {# <div class="col-md-4">{{form_row(form.slug)}}</div> #}
        {{form_rest(form)}}
                <button class="btn btn-primary form_btn mt-4">{{button|default('Enregistrer')}}</button>
            </div>
    {{form_end(form)}}